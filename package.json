{
  "name": "playwright-tripadvisor-pom",
  "version": "1.0.0",
  "description": "Playwright JavaScript POM Framework using Tripadvisor",
  "scripts": {
    "test": "npx playwright test",
    "test-headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}